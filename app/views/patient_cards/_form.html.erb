<%= form_for(patient_card) do |f| %>
  <% if patient_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(patient_card.errors.count, "error") %> prohibited this patient_card from being saved:</h2>

      <ul>
      <% patient_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %>
    <%= f.text_field :number %>
  </div>

  <div class="field">
    <%= f.label :clinic_id %>
    <%= f.text_field :clinic_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
